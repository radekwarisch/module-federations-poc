version: '0.2'

phases:
  install:
    commands:
      - 'npm install'
  build: 
    commands: 
      - 'npm run build:home --production'

artifacts: 
  base-directory: 'packages/home'
  files: 
    - '*'