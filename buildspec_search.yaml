version: '0.2'

phases:
  install:
    commands:
      - 'npm install'
  build: 
    commands: 
      - 'npm run build:search --production'

artifacts: 
  base-directory: 'packages/search'
  files: 
    - '*'